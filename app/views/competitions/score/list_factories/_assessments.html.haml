= f.input :next_step, as: :hidden, input_html: { value: 'names' }
= f.association :assessments, as: :check_boxes, collection: @list_factory.possible_assessments, label_method: :name_with_request_count, hint: 'Bitte alle Wertungen auswählen, die auf der Startliste erscheinen sollen.'

= f.primary_button :submit, 'Weiter', class: 'mt-3'
