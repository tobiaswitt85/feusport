= render 'devise/header'

.row
  .col-md-8
    = simple_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f|
      = card('Entsperr-E-Mail erneut zusenden') do |c|
        - c.body do
          = f.full_error :unlock_token
          = f.input :email, autofocus: true, required: true, input_html: { autocomplete: 'email' }
        - c.footer do
          = f.primary_button :submit, 'Zusenden'
  .col-md-4
    = card('Hinweise') do |c|
      - c.body do
        Wenn du die E-Mail nicht findest, schaue bitte auch in deinem Spam-Verzeichnis nach.
